import { PromptTemplate } from '@langchain/core/prompts'

export const EVAL_SUMMARY_PROMPT = `
You are an expert researcher evaluating summaries of a research paper.

You will be provided with:
- The original abstract of the research paper.
- Summaries generated by different models.

Evaluate each summary on how well it captures the main points of the original abstract.

Provide a score for each summary between 1 and 10.

# Original Abstract
\`\`\`txt
{abstract}
\`\`\`

# Summaries
\`\`\`txt
{summaries}
\`\`\`

Return the scores as a JSON object with the model names as keys and scores as values.
[IMPORTANT] Only return the JSON object.
`

export const EVAL_PROMPT_TEMPLATE = PromptTemplate.fromTemplate(EVAL_SUMMARY_PROMPT)
